{
  "name": "load_skill",
  "description": "Load an allowlisted Waykit role SKILL.md in the parent chat. Skills-only mode: do not launch a host subagent. Pass specialist id, capability class, and handover paths. Do not load stack profiles. Do not split TDD gear 1 and gear 2.",
  "inputSchema": {
    "type": "object",
    "required": ["specialist"],
    "properties": {
      "specialist": {
        "type": "string",
        "enum": [
          "agent-debug",
          "agent-xfn",
          "agent-review",
          "agent-security",
          "agent-arch-drift",
          "agent-spec",
          "agent-tdd"
        ],
        "description": "Allowlisted role skill to load in the parent"
      },
      "class": {
        "type": "string",
        "enum": ["plan", "review", "implement", "cheap"],
        "description": "Optional capability class from models/catalog.yaml"
      },
      "handoverPaths": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Handover files the parent must read or write"
      }
    },
    "additionalProperties": false
  }
}
