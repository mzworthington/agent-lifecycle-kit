{
  "name": "agent-lifecycle-kit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Modular agent lifecycle guardrails, skills, and tooling",
  "bin": {
    "kit": "./bin/kit",
    "agent-kit": "./bin/kit"
  },
  "scripts": {
    "prepare": "husky",
    "typecheck": "tsc --noEmit",
    "kit": "node --import tsx/esm bin/kit.ts",
    "scan": "./scripts/scan-skill-security.sh",
    "validate": "./scripts/validate-evals.sh",
    "verify": "./scripts/verify-skills-layout.sh",
    "export-rules": "./scripts/export-ide-rules.sh",
    "eval:run": "./scripts/run-evals.sh",
    "eval:edd": "node --import tsx/esm bin/kit.ts eval ci --suite evals/edd/architecture_routing.yaml --threshold-routing 95 --model scripted --out out/reports && node --import tsx/esm bin/kit.ts eval ci --suite evals/edd/kit_knowledge.yaml --threshold-routing 95 --model scripted --out out/reports",
    "test:edd": "node --import tsx/esm --test scripts/lib/edd/edd.test.ts",
    "test:kit-knowledge": "node --import tsx/esm --test mcps/servers/kit-knowledge/src/knowledge.test.ts",
    "measure-context": "./scripts/measure-context-budget.sh",
    "init-project": "./scripts/init-project.sh",
    "check": "pnpm run scan && pnpm run validate && pnpm run verify && pnpm run export-rules -- --check && pnpm run eval:run && pnpm run test:edd && pnpm run eval:edd && pnpm run test:kit-knowledge && pnpm run measure-context"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "ajv": "^8.20.0",
    "husky": "^9.1.7",
    "tsx": "^4.19.2",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "yaml": "^2.9.0",
    "zod": "^4.5.4"
  }
}
